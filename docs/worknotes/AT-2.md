# AT-2 working notes

- Goal: ship the §14 S1Atlas agent skill as the final V1 milestone, with evidence-first Schedule I methodology, real CLI/MCP surface references, verified Claude Code discovery/install guidance, and README/spec status updates.
- Branch: `feat/at-2-s1atlas-agent-skill`, based on `main` at `34ac819`.
- Ticket: AT-2, “Author S1Atlas agent skill for evidence-first modding workflows”.
- Brainstorm approval: canonical repo artifact is `skills/s1atlas/SKILL.md`; methodology only; CLI is always-available fallback; MCP requires registered stdio server; S1API/S1MAPI are CLI-only.
- Runtime discovery: `C:\Users\david\.claude\skills` exists; repo `.claude` currently contains only local settings; `claude` executable was not found. Verify the project-scoped path from runtime documentation before authoring installation text.
- Surface inventory: CLI command registrations are under `src/S1Atlas.Cli/Commands`; MCP registrations use `[McpServerTool(Name = ...)]` under `src/S1Atlas.Mcp/Tools`.
- Verification plan: RED pressure scenarios without the skill; GREEN load/trigger scenarios with the skill; static surface-name check against registrations (not README); frontmatter/path checks; README/spec coherence review.
- RED baseline: fresh agents were cautious about stale guides and missing MCP, but one proposed unshipped binary/runtime inspection and live-test workflow, and another cited general API documentation rather than indexed S1API/S1MAPI provenance. The skill must bind the response to shipped surfaces and identifiers.
- GREEN draft: `skills/s1atlas/SKILL.md` now requires current-build preference, source-before-behavior, API-before-direct-patch, update rechecks, explicit state/provenance labels, CLI fallback, and no Atlas writes.
- Discovery/load verification: project junction `C:\Users\david\Documents\MadJag Studios\S1Atlas\.claude\skills\s1atlas` resolved to `skills\s1atlas` and produced identical SHA-256 bytes; it was removed after the smoke test because the install is deployment-local. Existing `C:\Users\david\.claude\skills\starting-work` is a working junction, confirming the user-scoped Claude path convention.
- Trigger verification: a fresh agent loaded the project skill and, under the stale-guide/direct-patch/MCP-unavailable pressure scenario, used CLI `status`/`builds`/symbol/source queries, required S1API/S1MAPI checks, required `diff`, separated FACT/DERIVED/INTERPRETATION, and requested the correct provenance IDs. No Claude Code executable is installed in this environment, so this is a path-resolution plus fresh-agent load check rather than a live `claude` CLI invocation.
- Surface verification: CLI command registrations and `[McpServerTool(Name = ...)]` attributes were checked directly in `src/S1Atlas.Cli/Commands` and `src/S1Atlas.Mcp`; every surface named by the skill is registered and referenced.
- Review correction: separate game-update `diff`/`compare_symbol` guidance from API/dependency re-query guidance; explicitly reject running/failed/candidate/unverified authority; qualify runtime discovery as path/load verified without claiming a live Claude CLI invocation.
- Post-review verification: revised skill passed byte-identical project-path resolution, direct CLI/MCP registration audit (18 CLI names and 15 MCP names), and a fresh load/trigger smoke test that kept game diffing separate from API re-query and rejected unavailable authority.
